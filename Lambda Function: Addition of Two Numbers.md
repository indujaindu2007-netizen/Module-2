# Lambda Function in Python: Addition of Two Numbers

## 🎯 Aim
To write a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their sum.

## 🧠 Algorithm
1. Get two integer inputs from the user.
2. Use a **lambda function** to define a function `f` that returns `a + b`.
3. Call the function with the user inputs and print the result.

## 🧾 Program
```
x=int(input("Enter a Number:"))
y=int(input("Enter a Number:"))
f=lambda x,y:x+y
print("Sum:",f(x,y))
```

## Output
<img width="379" height="214" alt="image" src="https://github.com/user-attachments/assets/b8b4f00c-246a-4bd7-af65-1eeefe39cb49" />

## Result

Thus, the Python program successfully uses a lambda function to add two numbers and prints the correct sum.
