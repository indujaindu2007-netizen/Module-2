# Built-in Functions -Binary Conversion Using Built-in Functions in Python

## 🎯 Aim
To write a Python program to convert the number **16** into its **binary representation** using built-in Python functions.

## 🧠 Algorithm
1. Assign the value `16` to a variable `a`.
2. Use the built-in `bin()` function to convert the number to binary.
3. Print the result.

## 🧾 Program

```
a=5
print(bin(a))
```

## Output

<img width="355" height="141" alt="image" src="https://github.com/user-attachments/assets/cff2b281-c0a1-4afb-b7ab-7d6a2929cac5" />


## Result
Thus, the Python program successfully converts the number 16 into its binary representation using the built-in bin() function and prints the correct output.
